"""Package marker for configuration module.

This file makes the ``config`` directory a Python package so that
``import config.settings`` works as expected.  It intentionally
contains no runtime logic.
"""