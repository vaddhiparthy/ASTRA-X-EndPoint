# Example configuration for ASTRA‑X‑Aggregator

# Specify which language model provider to use.  Supported values: "ollama" or "openai".
LLM_PROVIDER=ollama

# Ollama settings (used when LLM_PROVIDER=ollama)
OLLAMA_HOST=http://host.docker.internal:11434
OLLAMA_MODEL=hola

# OpenAI settings (used when LLM_PROVIDER=openai)
#OPENAI_API_KEY=sk-your-api-key
#OPENAI_MODEL=gpt-4-1106-preview

# Override the system prompt loaded from config/prompt_static.txt and config/prompt_structure.txt.
# Leave empty to use only the prompt files.
SYSTEM_PROMPT=

# The name of the chatbot as it appears in the UI header.
CHATBOT_NAME=ASTRA‑X‑Aggregator